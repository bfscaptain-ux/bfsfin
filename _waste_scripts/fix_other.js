const fs = require("fs"); let code = fs.readFileSync("src/components/SmartBot.tsx", "utf8"); const replacement = `      case "BT_CURRENT_BANK":\n        if (text === "Other Bank/NBFC" || text.toLowerCase().includes("other")) {\n           triggerBotMessage("Kripya apne us Bank ya NBFC ka naam bataein:");\n           setCurrentStep("BT_OTHER_BANK_NAME");\n        } else {\n           setLeadData(prev => ({ ...prev, btBank: text }));\n           triggerBotMessage("Aur wahan abhi aapka interest rate (ROI) lagbhag kitna chal raha hai?", ["8.5% - 9%", "9% - 10%", "10% se zyada", "Nahi Pata"]);\n           setCurrentStep("BT_CURRENT_ROI");\n        }\n        break;\n\n      case "BT_OTHER_BANK_NAME":\n        setLeadData(prev => ({ ...prev, btBank: text }));\n        triggerBotMessage("Aur wahan abhi aapka interest rate (ROI) lagbhag kitna chal raha hai?", ["8.5% - 9%", "9% - 10%", "10% se zyada", "Nahi Pata"]);\n        setCurrentStep("BT_CURRENT_ROI");\n        break;`; code = code.replace(/case "BT_CURRENT_BANK":[\s\S]*?break;/, replacement); fs.writeFileSync("src/components/SmartBot.tsx", code);
